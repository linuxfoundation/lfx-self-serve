// Copyright The Linux Foundation and each contributor to LFX.
// SPDX-License-Identifier: MIT

/** Cookie storing the user's selected foundation, persisted across refreshes (board-scoped lenses). */
export const SELECTED_FOUNDATION_COOKIE_KEY = 'lfx-selected-foundation';

/** Cookie storing the user's selected project, persisted across refreshes (project-scoped lenses). */
export const SELECTED_PROJECT_COOKIE_KEY = 'lfx-selected-project';
