// Copyright The Linux Foundation and each contributor to LFX.
// SPDX-License-Identifier: MIT

/** Optional tweaks for `buildChartExternalTooltip`. */
export interface ChartExternalTooltipOptions {
  /** Appended to each formatted tooltip value (e.g. `' days'`). */
  valueSuffix?: string;
}
